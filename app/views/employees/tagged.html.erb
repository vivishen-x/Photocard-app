<% provide(:title, "Result") %>
<div class="container-fluid center">
	<h1><%= @employees.count %> Employees</h1>
  <h3>Tag: <span class="label label-primary"><%= params[:tag] %></span>
	</h3>
	<%= will_paginate %>

		<div class="employee-lineup">
			<div class="row">
        <%= render @employees %>
			</div>
		</div>

	<%= will_paginate %>
</div>
